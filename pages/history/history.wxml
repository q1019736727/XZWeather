<!--pages/history/history.wxml-->
<view>
  <view class='cityItem' wx:for='{{cityList}}' wx:key='{{index}}' bindtap='clickAddress' data-index='{{index}}'>
    <text>{{item}}</text>
    <icon class='iconfont {{item === preAddressName ? "icon-city":""}}'></icon>
  </view>
</view>